// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // redondea, arriba desde 2.5 y abajo desde 2.4
resultado = Math.ceil(2.2); // ceil siempre redondea hacia arriba
resultado = Math.floor(2.9); //Floor siempre redondea hacia abajo
resultado = Math.sqrt(144); //raiz cuadrada
resultado = Math.abs(-200); // vuelve numeros positivos
resultado = Math.min(3, 5, 6, 7, 2, 6, 10); // num maximo
resultado = Math.max(3, 5, 6, 7, 2, 6, 10); // num minimo
resultado = Math.random(); // crea numeros ramdon menores a 1
resultado = Math.floor(Math.random() * 30); // unir dos math

console.log(resultado);
